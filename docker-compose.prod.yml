services:
  backend:
    build: ./backend
    container_name: credi-digital-backend
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - backend/.env
    environment:
      NODE_ENV: production
